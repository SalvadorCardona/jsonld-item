---
"jsonld-item": minor
---

Initial release: helpers for JSON-LD / Hydra items and collections — read an
IRI or a type from either a string or an object, extract an id, derive a type
from a URI, build items and collections. No runtime dependency. Covered by
24 tests.
